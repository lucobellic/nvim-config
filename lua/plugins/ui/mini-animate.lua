return {
  'echasnovski/mini.animate',
  -- enabled = not vim.g.neovide,
  enabled = false,
  opts = {
    cursor = {
      enable = false,
    },
    scroll = {
      enable = true,
    },
    resize = {
      enabled = false,
    }
  },
}
